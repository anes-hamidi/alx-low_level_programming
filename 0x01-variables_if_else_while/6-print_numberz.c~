#include <stdio.h>
/**
 * main - Entry point
 * Discription: print numberz
 * Return: Always  0  (success)
 */
int main(void)
{
for (int i = '0'; i <= '9'; i++)
putchar(i);
putchar('\n');
return (0);
}
