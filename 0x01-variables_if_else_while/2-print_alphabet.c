#include <stdio.h>
/**
 * main - Entry point
 * Discription: print alphabet in lowercase
 * Return: Always 0 (success)
 */
int main(void)
{
char l;
for (l = 'a'; l <= 'z'; l++)
putchar(l);
putchar('\n');
return (0);
}
