#include "main.h"
/**
 * mul - multiplies two integer
 *@a: number 1
 *@b: number 2
 *@result: result of functtion
 * return: Always 0 (success)
 */
int mul(int a, int b)
{
int result = a * b;
return (result);
}
